import { Grid2, styled } from "@mui/material";

export const GridContainer = styled(Grid2)(() => ({
  width: '1320px',
  maxWidth: '90%',
  height: '100vh',
  margin: '0 auto',
  padding: '1rem 0'
}));